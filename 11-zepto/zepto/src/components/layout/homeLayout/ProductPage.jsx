import React from "react";

const ProductPage = ({ category }) => {
  console.log(category);

  return (
    <div>
      {/* {category === "all" && }
      {category === "all"}
      {category === "all"} */}
    </div>
  );
};

export default ProductPage;
